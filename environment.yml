# To create the conda environment, run:
#   conda env create -f environment.yml
name: lg-chatbot
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.13.3\
  - langgraph=0.4.4
  - langsmith=0.3.39
  - pip
  - pip:
      - autopep8
      - flake8
      - "-e ."
